---
import { Button } from '@/components/ui/button';
import { MobileDrawer } from '@/components/react/MobileDrawer';
import { Phone } from 'lucide-astro';
import { routes } from '@/routes/routes';

// Get current path to determine if we're on home page
const currentPath = Astro.url.pathname;
const isHomePage = currentPath === '/';
---

<div class="absolute left-0 top-0 z-10 w-full">
  {/* Mobile Menu */}
  <MobileDrawer client:load />
  <a class="absolute right-6 top-6 md:hidden" href="/">
    <Phone size={32} />
  </a>

  {
    !isHomePage && (
      <a href="/" class="absolute left-1/2 top-6 -translate-x-1/2 transform">
        <img src="/logo.png" alt="Logo" class="h-24 mix-blend-lighten" />
      </a>
    )
  }

  {/* Desktop Menu */}
  <div class="hidden gap-4 p-6 md:flex">
    {
      routes.map((route) => (
        <Button variant="link" size="sm">
          <a class="text-2xl text-white" href={route.path}>
            {route.label}
          </a>
        </Button>
      ))
    }
    <Button variant="link" size="sm" className="ml-auto">
      <a class="text-2xl text-white" href="/">Prenota</a>
    </Button>
  </div>
</div>
